Вот содержимое файла .gitignore, которое вы можете создать в корне вашего репозитория. Он включает в себя стандартные исключения для Node.js проектов, а также некоторые общие файлы, которые не должны попадать в Git (включая остатки PHP-конфигурации).# Node.js
node_modules/
npm-debug.log
.env
build/
dist/
*.tgz
.DS_Store
*.log
/form_submissions.log # Лог файл из вашего PHP проекта
.env.local
.env.development.local
.env.test.local
.env.production.local
.vscode/ # Если используете VSCode и хотите игнорировать его настройки

# PHP-специфичные (если остались какие-то старые файлы)
*.php~
*.bak
*.old
*.tmp
# config.php # Если вы больше не используете PHP, и не хотите, чтобы этот файл был в репозитории

# IDEs and OS
.idea/ # IntelliJ IDEA
.vscode/ # Visual Studio Code
.DS_Store # macOS
Thumbs.db # Windows

# Coolify / Deployment artifacts (Coolify генерирует их)
.coolify/
artifacts/
Объяснение:node_modules/: Папка, содержащая все установленные зависимости Node.js. Она генерируется командой npm install и не должна храниться в Git.npm-debug.log: Лог-файл, создаваемый npm в случае ошибок..env: Файл, содержащий переменные окружения (например, ключи API, токены). Эти данные должны быть конфиденциальными и не должны попадать в Git-репозиторий. В Coolify вы добавляете эти переменные отдельно.build/, dist/: Часто используемые папки для скомпилированного или собранного кода, который также не нужно хранить в Git.*.tgz: Архивы пакетов, которые могут быть созданы npm..DS_Store, Thumbs.db: Скрытые файлы операционных систем macOS и Windows соответственно, содержащие метаданные папок.*.log, /form_submissions.log: Файлы логов, которые генерируются приложением и не являются частью исходного кода.PHP-специфичные файлы: Включены для очистки, если у вас остались какие-либо резервные копии или временные файлы от старой PHP-разработки..coolify/, artifacts/: Папки, которые могут быть созданы Coolify или вашей системой развертывания и не должны быть частью вашего исходного кода.Создайте новый файл с именем .gitignore (обратите внимание на точку в начале) в корне вашего репозитория и вставьте в него этот код.